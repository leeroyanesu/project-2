/** @type {import('next').NextConfig} */
const nextConfig = {
   
    i18n:{
        locales:['en-US','de','fr'],
        defaultLocale:'en-US',
        localeDetection:false
    }
}

module.exports = nextConfig
